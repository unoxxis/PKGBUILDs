# Maintainer: Boris Wezisla <uno@unoxxis.net>
pkgname=unx-laptop
pkgver=2
pkgrel=2
pkgdesc="Unoxxis Metapackage for laptop systems"
arch=('any')
url="https://github.com/unoxxis/PKGBUILDs"
license=('GPL-3.0-or-later')
groups=(unoxxis-meta)

# Base System Packages
depends=(unx-desktop)

# Packages to connect to home VPN
depends+=(networkmanager-l2tp strongswan networkmanager-strongswan)

# source=()

# sha256sums=()

# install="$pkgname.install"

#package() {
#    
#}